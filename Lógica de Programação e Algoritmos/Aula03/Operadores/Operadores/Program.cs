//Operadores relacionais    
/* < | menor que
   > | maior que
   <= | menor ou igual a
   >= | maior ou igual a
   == | igual a
   != | diferente de
*/

/*
   Operadores relacionais são usados para comparar valores e retornar um resultado booleano (verdadeiro ou falso). 
   Eles são comumente utilizados em estruturas de controle de fluxo, como if, while e for, para tomar decisões com base em condições.
*/

/*
int x = 1;
int y = 2;
Console.WriteLine(x<y); // true
Console.WriteLine(x<=y);
Console.WriteLine(x<=y);
Console.WriteLine(x>=y);

char a = 'S';
char b = 'N';    
Console.WriteLine(a==b);
Console.WriteLine(a!=b);
Console.WriteLine(a==b? "Verdadeiro" : "OK");
*/

// Operadores lógicos - Comparar valores booleanos
/* & | E (Lógico)
   || | OU (Lógico)
   !  | NÃO (Negação Lógica)
   ^  | OU Exclusivo (XOR)
   &&  | E (Lógico Condicional)
   || | OU (Lógico Condicional)
*/
/*
bool x = true;
bool y = false;

Console.WriteLine(x & y);
*/
